<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="member">

	<select id="selectId" resultType="Member" >
		select * from member where member_id = #{memberId}
	</select>
	
<insert id="enroll"> insert into TB_MEMBER_INFO values( #{memberId}, #{memberPwd}, #{memberName}, #{nickname}, #{birthday}, #{phone}, #{pwdHintCode}, #{pwdAns}, #{address}, #{email}, #{gender}, #{grade}, #{age}, #{mateId}, #{childNumber}, default ) </insert>

<select id="selectOne" resultType="member"> select * from TB_MEMBER_INFO where member_Id = #{memberId} </select>
	
</mapper>